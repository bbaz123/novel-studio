# P1 生成的基线数据与隔离库副本不入库：
#   - 数据库副本体积大（data/ ~0.9MB、stress-data/ ~2.7MB）且可由脚本重建；
#   - 逐用例的原始 JSON 基线（约 5MB）可由 capture-baseline.mjs 重新生成。
# 保留：工具、README、以及体积很小的 summary.json（人可读的证据）。
data/
stress-data/
logs/
baselines*/*.json
!baselines*/summary.json

# ── 隔离实例的临时运行产物（2026-09-16 补）──────────────────────────────
# ⚠️ 这条是补漏：早先只忽略了 data/ 与 stress-data/，**漏了 gate-data/**。
# 结果 `git add -A` 把隔离实例的 SQLite 三件套（含 1.9MB 的 WAL）提交进了仓库——
# 提交后才发现（`create mode 100644 .p1-baseline/gate-data/novel.db`）。
# 教训：忽略清单要和"工具实际会在哪儿生成什么"对齐；新工具引入新目录时要同步加。
# 与 .p6-cutover/snapshot.mjs 的 EXCLUDES 保持同一份语义（那边是快照的排除规则）。
gate-data/
suite-data/
.rehearsal/
.verify/
.negctl*
gate-env.json
gate-env.stop
blackhole-*.jsonl
.blackhole-*.jsonl
# 假 LLM 端点的请求日志：每次验收都会重建，且**刻意记录请求形状**（模型/流式/字数），
# 属于临时证据而不是资产。⚠️ 补这条的原因与上面 gate-data/ 同源：新工具引入新产物，
# 忽略清单没跟上，`git add -A` 就会把它的运行残留提交进来。
fake-llm*.jsonl
.exp-settings/

# 回滚矩阵的临时 worktree（跑完自删；崩了才会留下）
.revert-matrix/

# 人工盲测（blind-ab.mjs）的输出目录：A.md / B.md / mapping.json / result.json
# 都是临时对比材料，不入库——否则每跑一次就多 4 个"没人认领"的文件，阶段映射会报出来。
.blind/